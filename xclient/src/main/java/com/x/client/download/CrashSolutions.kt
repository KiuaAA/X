package com.x.client.download

object CrashSolutions {
    fun suggest(message: String): String {
        // Real mapping table comes in the crash-parser part.
        return "Check your internet connection and storage permissions, then retry."
    }
}
